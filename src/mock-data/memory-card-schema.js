const memoryCardSchema = {
   id: String,
   imagery: String,
   answer: String,
   userId: String,
   createdAt: Number,
   nextAttempt: Number,
   lastAttemptAt: Number,
   totalSuccessfulAttemps: Number,
   level: Number,
};
